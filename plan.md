# 구현 계획 (Implementation Plan)

이 문서는 [PRD.md](PRD.md)의 요구사항을 실제로 어떻게 구현했는지 정리한 기술 문서입니다. "무엇을·왜"는 PRD.md, "어떻게"는 이 문서를 보면 됩니다.

## 1. 기술 스택 선택

| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router) | 화면과 서버 로직(서버 액션, 라우트 핸들러)을 한 프로젝트로 처리, Vercel 배포와 궁합 |
| 언어 | TypeScript (strict) | 계산 로직의 타입 안정성 확보 |
| 스타일 | Tailwind CSS 4 | 별도 CSS 파일 없이 빠른 UI 반복 |
| 인증 | Supabase Auth | 별도 DB 서버 구축 없이 로그인 게이트만 필요했기 때문 |
| 배포 | Vercel | Next.js와 사실상 표준 조합, CLI로 무중단 배포 |

## 2. 계산 엔진 — `src/lib/roi.ts`

- 모든 계산은 **순수 함수**로 작성 (`simulate`, `simulateCore`, `calcBreakEvenCases`). 같은 입력이면 항상 같은 결과가 나오도록 해 PRD의 "계산 오차 0%" 요구를 코드 구조로 보장했다.
- 월별 손익을 반복 계산하면서, 무상 제공 소모품을 먼저 소진한 뒤부터 소모품 비용을 반영한다.
- 손익분기 건수는 무상 제공 구간과 유상 구간을 나눠서 역산한다.
- 보수/공격 시나리오는 같은 `simulateCore`를 월 시술 건수만 ±30% 조정해 재사용한다 — 로직 중복을 피하기 위해서다.

## 3. 화면 구조

- `src/app/page.tsx` — 서버 컴포넌트. 로그인 여부만 확인하고 실제 UI는 렌더링하지 않는다.
- `src/components/RoiCalculator.tsx` — 클라이언트 컴포넌트. 계산기 전체 로직과 상태 관리.
- 상담 현장에서는 "미리 채워진 결과부터 보여주고 설명"하는 흐름이라, 결과 섹션(요약 카드·그래프·표)을 입력 폼보다 위에 배치했다.
- 차트는 별도 라이브러리 없이 SVG로 직접 구현 (`CumulativeChart.tsx`) — 불필요한 의존성을 늘리지 않기 위해서다.
- 색상 팔레트는 색맹 판별 검증 스크립트로 통과 여부를 확인한 뒤 고정했다.

## 4. 인증 설계 — Supabase Auth

### 4.1 기본 로그인: 매직링크 (비밀번호 없음)

- 영업팀 계정은 회원가입 화면 없이 관리자가 미리 생성한다.
- 로그인은 이메일만 입력 → `signInWithOtp({ shouldCreateUser: false })`로 등록된 이메일에만 링크를 보낸다.
- **Next.js 16 함정**: `middleware.ts`가 16버전부터 `proxy.ts`로 이름이 바뀌었다. 세션 갱신은 `src/proxy.ts`에서 처리한다.
- **콜백 처리**: 이 프로젝트는 Supabase 무료 요금제라 이메일 템플릿을 직접 커스터마이즈할 수 없다. 그래서 `token_hash` 방식(`/auth/confirm`)이 아니라, Supabase가 자체 호스팅하는 검증 페이지를 거쳐 돌아오는 **PKCE `code` 교환 방식**(`/auth/callback` + `exchangeCodeForSession`)을 사용한다.
- **알려진 제약**: PKCE 특성상 "링크를 요청한 브라우저/기기"와 "링크를 클릭하는 브라우저/기기"가 같아야 한다. 데스크톱에서 요청하고 휴대폰에서 열면 실패한다.

### 4.2 관리자 전용: 비밀번호 로그인

- 이메일 링크를 기다릴 필요 없이 즉시 로그인하고 싶다는 요구로 `/login/admin` 경로를 별도로 추가했다.
- 허용 이메일 목록은 `ADMIN_EMAILS` 환경변수로 관리한다 — 소스코드(퍼블릭 저장소)에 실제 이메일을 남기지 않기 위해서다.
- 계정에 비밀번호가 남아 있어도(최초 일괄 생성 시 부여) `ADMIN_EMAILS`에 없으면 이 경로로 로그인할 수 없도록 서버 액션에서 먼저 걸러낸다.

## 5. 인쇄(A4) 지원

- `globals.css`에 인쇄 전용 스타일을 분리해, 화면용으로 커진 폰트·여백을 인쇄 시에는 축소한다.
- 장비 4대를 비교해도 A4 한 장 안에 들어가도록 실제 렌더링 높이를 스크립트로 측정하며 맞췄다.

## 6. 배포 파이프라인

- GitHub: `gh` CLI 없이 REST API로 저장소를 생성하고, 커밋 시점에만 토큰을 URL에 담아 푸시한 뒤 즉시 깨끗한 origin으로 되돌린다 (토큰이 `.git/config`에 남지 않도록).
- Vercel: CLI(`npx vercel@latest`)로 배포. 환경변수는 앱이 실제로 참조하는 값만 등록한다 (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `ADMIN_EMAILS`) — 관리용 토큰(`GITHUB_TOKEN`, `VERCEL_TOKEN`, `SUPABASE_ACCESS_TOKEN`)은 절대 등록하지 않는다.

## 7. 의도적으로 하지 않은 것

- 비밀번호 재설정/변경 화면 — 계정 수가 적고 관리자가 직접 재발급하는 편이 더 간단하다고 판단.
- 상담 이력·고객 데이터 저장 — PRD 비범위. 계산은 항상 세션 안에서만 이뤄진다.
- 사용자별 권한 등급 — 로그인 여부만 다르고, 로그인 후 화면·기능은 모든 사용자에게 동일하다.

## 8. 남은 리스크 / 향후 고려 사항

- 관리자 비밀번호가 단순한 값으로 설정돼 있다 — 필요 시 강화 권장.
- Supabase 무료 요금제의 이메일 발송 한도 — 사용자·로그인 빈도가 늘면 유료 플랜 또는 커스텀 SMTP 전환을 검토해야 한다.
- 소스코드에 실제 장비명·가격이 포함돼 있다 (사용자 결정으로 공개 유지).
